{
  "name": "@potato-hash/groundskeeper",
  "version": "0.1.0",
  "description": "Autonomous agent shell for Espalier and OMP: durable tasks, approvals, schedules, channels, and worker orchestration.",
  "type": "module",
  "license": "MIT",
  "bin": {
    "groundskeeper": "./dist/src/app/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc --noEmit -p tsconfig.json",
    "test": "npm run build && node --test dist/test/*.test.js"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3"
  }
}