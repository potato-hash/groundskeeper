# Context References

Use LCM references instead of guessing from compacted context.

- Search with `lcm_grep` or `lcm_expand_query` when evidence may be far back.
- Use `lcm_describe` for summary nodes and `lcm_expand` for raw turns.
- Cite the recovered turn/node id in traces.
- If memory is degraded, say so and avoid fabricated detail.
